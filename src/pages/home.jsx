import React from 'react';
import { Button } from '@mui/material';

const Home = () => {
    return (
        <div>
            <h1>Welcome to Recuvero</h1>
            <Button variant="contained" color="primary">
                Click Me
            </Button>
        </div>
    );
};

export default Home; 